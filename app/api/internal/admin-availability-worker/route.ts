import { boundedWorkerBatch, validWorkerAuthorization } from "@/lib/admin/availability/worker";
import { getAvailabilityAdminClient } from "@/lib/admin/availability/repository";
export const runtime="nodejs"; export const dynamic="force-dynamic";
export async function POST(request:Request){ if(!validWorkerAuthorization(request.headers.get("authorization"),process.env.ADMIN_AVAILABILITY_WORKER_SECRET))return Response.json({ok:false},{status:401}); let body:unknown={}; try{body=await request.json();}catch{} const batch=boundedWorkerBatch(body&&typeof body==="object"&&"batchSize" in body?(body as {batchSize?:unknown}).batchSize:undefined); const admin=getAvailabilityAdminClient(); if(!admin.ok)return Response.json({ok:false},{status:503}); const {data,error}=await admin.client.rpc("run_due_admin_dish_availability",{p_worker_id:"primary",p_batch_size:batch}); if(error)return Response.json({ok:false},{status:503,headers:{"cache-control":"no-store"}}); return Response.json({ok:true,processed:data},{headers:{"cache-control":"no-store"}}); }
